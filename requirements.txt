fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# AI and embeddings
sentence-transformers==2.2.2
huggingface_hub==0.23.2
torch==1.13.1
--extra-index-url https://download.pytorch.org/whl/cpu
transformers==4.44.2

# Token counting
tiktoken==0.7.0

# File parsing
pypdf==4.3.1

# LLM + API (Groq + HTTP stack)
groq==0.11.0
httpx==0.27.2   # ✅ works with groq==0.11
# (no httpcore line needed — httpx will install a compatible 1.x version automatically)

# Vector store
faiss-cpu

# Optional utils
scikit-learn
numpy
requests==2.32.3
